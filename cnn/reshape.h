//#ifndef _RESHAPE_H_
//#define _RESHAPE_H_
//
//#include "features.h"
//#include "kernels.h"
//
////bool features::reshape(features& tsr, matrix& fts_matrix, int reshape_mode = FEATURES2MATRIX, int kernels_rows = KERNEL_ROWS, \
//	int kernels_cols = KERNEL_COLS, int stride = 1, int padding_mode = VALID_PADDING);
//matrix reshape(features& features, int kernels_rows = KERNEL_ROWS, \
//	int kernels_cols = KERNEL_COLS, int stride = 1, int padding_mode = VALID_PADDING);
//
//features reshape(matrix& features_mat, int kernels_rows = KERNEL_ROWS, \
//	int kernels_cols = KERNEL_COLS, int stride = 1, int padding_mode = VALID_PADDING);
//
//matrix reshape(kernels& kers_);
//kernels reshape(matrix& kers_mat_);
//
//#endif